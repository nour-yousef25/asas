import { readFileSync, statSync } from "node:fs"; const file = process.argv[2]; const ids = ["PORT-R01", "PORT-R02", "PORT-R03", "PORT-R04", "PORT-R05", "PORT-R06"]; if (!file) throw new Error("EVIDENCE_PATH_REQUIRED"); const payload = JSON.parse(readFileSync(file, "utf8")); const evidence = Array.isArray(payload.evidence) ? payload.evidence : []; const observed = evidence.map((item) => item.id); const missing = ids.filter((id) => !observed.includes(id)); const duplicate = observed.filter((id, index) => observed.indexOf(id) !== index); const failed = evidence.filter((item) => item.result !== "PASS").map((item) => item.id); const mode = statSync(file).mode & 0o777; const valid = payload.status === "PASS_W02_PORTABILITY_REHEARSAL" && evidence.length === ids.length && missing.length === 0 && duplicate.length === 0 && failed.length === 0 && payload.cleanup?.ok === true && payload.cleanup?.residueCount === 0 && payload.credentialsPersisted === false && payload.productionResourcesTouched === false && payload.providerHaDrScaleProven === false && mode === 0o600; console.log(JSON.stringify({ valid, required: ids.length, observed: evidence.length, missing, duplicate: [...new Set(duplicate)], failed, cleanup: payload.cleanup, mode: mode.toString(8) }, null, 2)); process.exitCode = valid ? 0 : 2;

import { readFileSync, statSync } from "node:fs";
const file = process.argv[2]; const ids = ["MGR-F01", "MGR-F02", "MGR-F03", "MGR-F04", "MGR-F05", "MGR-F06", "MGR-F07", "MGR-F08", "MGR-F09", "MGR-F10"]; if (!file) throw new Error("EVIDENCE_PATH_REQUIRED"); const payload = JSON.parse(readFileSync(file, "utf8")); const evidence = Array.isArray(payload.evidence) ? payload.evidence : []; const observed = evidence.map((item) => item.id); const missing = ids.filter((id) => !observed.includes(id)); const duplicate = observed.filter((id, index) => observed.indexOf(id) !== index); const failed = evidence.filter((item) => item.result !== "PASS").map((item) => item.id); const mode = statSync(file).mode & 0o777; const valid = payload.status === "PASS_FINANCIAL_MIGRATION_REHEARSAL" && evidence.length === ids.length && missing.length === 0 && duplicate.length === 0 && failed.length === 0 && payload.cleanup?.ok === true && payload.cleanup?.residueCount === 0 && payload.credentialsPersisted === false && payload.productionResourcesTouched === false && payload.rawGucIdentityUsed === false && mode === 0o600; process.stdout.write(`${JSON.stringify({ valid, required: ids.length, observed: evidence.length, missing, duplicate: [...new Set(duplicate)], failed, cleanup: payload.cleanup, mode: mode.toString(8) }, null, 2)}\n`); process.exitCode = valid ? 0 : 2;

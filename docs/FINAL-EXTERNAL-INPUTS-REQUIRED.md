# FINAL EXTERNAL INPUTS REQUIRED — ASAS Plus

كل الملفات الآتية تخص **Production**، وتبقى خارج Git وlogs وevidence. المسار الأساسي المقترح هو `/opt/asasplus/shared/production-requests/` للطلبات غير السرية (`root:root 0600`) و`/opt/asasplus/shared/` للمادة التشغيلية وفق الجدول. لا ترسل القيمة في chat أو CLI history.

| Gate | الاسم الدقيق والغرض | مكان الحفظ وأقل صلاحية | طريقة الاختبار | التدوير/الإبطال |
|---|---|---|---|---|
| Storage | `TENANT_STORAGE_REFERENCE_DIRECTORY` و`TENANT_STORAGE_CREDENTIAL_DIRECTORY`: مرجع `secretref:` وcredential JSON منفصل لكل tenant. `ASAS_STORAGE_AUDIT_PROBE_REQUEST_FILE`: `organizationId`,`artifactId`,`objectKey`,`approvalReference`. | directories: `/opt/asasplus/shared/production-tenant-storage-{references,credentials}`، `root:asasplus 2750`; files `root:asasplus 0640`. IAM: bucket المحدد وprefix `private/<tenant>/` فقط؛ Put/Get/DeleteObject وHeadObject فقط عند الحاجة، دون public ACL. | signed HEAD على object audit مملوك للـtenant ثم lifecycle A/B معتمد، بلا URLs عامة. | أضف key/ref جديداً، اختبر، حدّث ref، ثم أبطل السابق من provider واحذف ملفه. |
| Auth — اختيار المسار | `ASAS_AUTH_LAUNCH_MODE=BOOTSTRAP` أو `IDP`. | systemd EnvironmentFile root-only، لا قيمة سرية. | preflight يرفض غياب الاختيار. | غيّر المسار فقط عبر Go/No-Go جديد. |
| Bootstrap | `ASAS_BOOTSTRAP_ADMIN_REQUEST_FILE`: `requestId`,`organizationId`,`email`,`approvalReference`. ثم `ASAS_BOOTSTRAP_ADMIN_COMPLETION_EVIDENCE_FILE`: `requestId`,`organizationId`,`administratorId`,`verificationReference`. | request/evidence في production-requests، `root:root 0600`. لا password في الملف ولا ينشئ agent user production. | provision/login بشري معتمد ثم evidence redacted؛ harness محلي لا يعد نجاحاً إنتاجياً. | revoke request/evidence عند الإلغاء؛ rotate `AUTH_SECRET` وفق session invalidation plan. |
| IdP | `ASAS_IDP_PRODUCTION_REQUEST_FILE`؛ `OIDC_ISSUER`,`OIDC_CLIENT_ID`,`OIDC_CLIENT_SECRET_FILE`,`OIDC_REDIRECT_URI`. | request `0600`; secret client منفصل root:root `0600`; redirect allow-list production فقط وscope `openid`/identity الأدنى. | metadata/JWKS أو SAML metadata read-only، login واحد معتمد، وnegative state/nonce/revocation. | أضف secret جديداً، اختبر callback، ثم revoke القديم من IdP. |
| Mail | `ASAS_MAIL_PRODUCTION_REQUEST_FILE` (`providerKey`,`fromAddress`,`fromName`,`approvalReference`)، `ASAS_MAIL_TRANSPORT_CONFIG_PATH`، و`ASAS_MAIL_DELIVERY_ENABLED=true` بعد approval. | config/secret reference `root:root 0600`; sender domain verified؛ send-only scope ولا mailbox/admin scope. | provider identity/probe ثم رسالة sandbox واحدة بعد موافقة مستقلة؛ preflight لا يرسل. | rotate token، re-probe، ثم revoke القديم. |
| Payments | **واحد فقط**: `ASAS_PAYMENT_EXCLUSION_APPROVAL_FILE` (`scope`,`owner`,`approvalReference`) للاستبعاد؛ أو `ASAS_PAYMENTS_LAUNCH_SCOPE=REQUIRED` مع `ASAS_PAYMENT_PRODUCTION_REQUEST_FILE`,`PAYMENT_PROVIDER_CONFIG_FILE`,`PAYMENT_WEBHOOK_LEDGER_APPROVAL_FILE`. | requests/config root:root `0600`; merchant scope أدنى، webhook secret منفصل، callback allow-list. | إن كان مطلوباً: sandbox signature negative + tenant-bound idempotency/reconciliation؛ لا charge حقيقي قبل approval. | rotate API/webhook credentials وفق provider، وأبطل القديم بعد reconciliation. |
| License | `ASAS_LICENSE_ACTIVATION_REQUEST_FILE` (`certificateFile`,`keyringFile`,`revocationFile`,`instanceId`,`approvalReference`) و`ASAS_LICENSE_REQUIRED=true` مع paths الفعلية. | certificate/keyring/revocation تحت `/opt/asasplus/shared/production-license/` root:root `0600`; keyring public-only؛ private signing key **لا يوضع على VPS**. | load محلي: Ed25519/expiry/edition/instance/revocation، ثم negative expiry/revoke. | keyring overlap قصير، توقيع cert جديد، ثم انزع key revoked. |
| Scheduler | `ASAS_DOMAIN_SCHEDULER_APPROVAL_FILE` (`owner`,`approvalReference`,`catalogueVersion`)، `ASAS_DOMAIN_SCHEDULER_CATALOGUE_PATH`، `ASAS_SCHEDULER_HEARTBEAT_PATH`، `ASAS_SCHEDULER_MAX_LAG_SECONDS`. | catalogue/approval/heartbeat تحت `/opt/asasplus/shared/production-scheduler/`، root:root `0600` للـcatalogue/approval وheartbeat بلا world-write. لا credentials عامة في service. | `pnpm run scheduler:dry-run`، ثم heartbeat/lag health؛ لا timer حي قبل approval منفصل. | pause service/timer، احتفظ بسجل failure، واستبدل catalogue بالإصدار المعتمد ثم revoke القديم. |
| Owner/window | `ASAS_GO_NO_GO_APPROVAL_FILE`: `owner`,`maintenanceWindowUtc`,`approvalReference`. | production-requests root:root `0600`. | preflight schema/permission فقط. | انتهاء النافذة أو سحب approval يعيد No-Go. |

يظل المخرج المطلوب بعد إدخال كل ما سبق **مراجعة Go/No-Go جديدة**، لا نشر تلقائي.
